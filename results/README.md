# Results

This directory is populated by the experiment drivers. Its contents
(CSVs, JSON summaries, GraphML samples) are gitignored to keep the
repo small. Reproduce by running the drivers in `../experiments/`.
